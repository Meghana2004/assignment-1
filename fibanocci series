#include <stdio.h>
int main()
{
   int a, b, c, i, n;
   printf("enter a number");
   scanf("%d",&n);
   a = b = 1;
   printf("%d %d ",a,b);
   for(i = 0; i <= n-2; i++) {
      c = a + b;
      printf("%d ", c);
      a = b;
      b = c;
   }
}
